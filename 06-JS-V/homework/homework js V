Prototype: son un mecanismo mediante el cual los objetos en JavaScript 
heredan características entre sí.

Constructor (de clases): es una plantilla que permite crear objetos a 
partir de ella. En programación orientada a objetos se denomina 
constructor al código que se ejecuta cuando se crea un objeto de un tipo 
determinado (a cada objeto creado se le denomina instancia).